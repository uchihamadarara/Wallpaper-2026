🔥 Firebase Firestore Rules:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // User Profiles
    match /users/{userId} {
      // Users can only read and write their own profile
      allow read, write: if request.auth != null && request.auth.uid == userId;
      // Admin can read all users
      allow read: if request.auth != null && request.auth.token.email == 'royal2781925@gmail.com';
    }

    // Wallpapers
    match /wallpapers/{wallpaperId} {
      // Anyone can read wallpapers
      allow read: if true;
      // Only Admin can write/delete wallpapers
      allow write: if request.auth != null && request.auth.token.email == 'royal2781925@gmail.com';
    }
    
    // Wallpaper Stats (Public aggregated data)
    match /wallpaper_stats/{wallpaperId} {
      // Anyone can read stats (likes, views)
      allow read: if true;
      // Users can update stats (like incrementing likes)
      allow write: if request.auth != null;
    }
  }
}

🔥 Firebase Realtime Database Rules:

{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.email === 'royal2781925@gmail.com')",
        ".write": "auth != null && (auth.uid === $uid || auth.token.email === 'royal2781925@gmail.com')"
      }
    },
    "wallpapers": {
      ".read": "true",
      ".write": "auth != null && auth.token.email === 'royal2781925@gmail.com'"
    },
    "wallpaper_stats": {
      ".read": "true",
      ".write": "auth != null"
    }
  }
}
